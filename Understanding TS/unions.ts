function sum(n1: string | number, n2: string | number) {
  return n1;
}

type TypeObj = "type1" | "type2";

const obj: {
  name: string;
  type: TypeObj;
} = {
  name: "",
  type: "type2",
};

type tipoAlgumaCoisa = {
  id: number;
  name: "";
};

enum AlgumaCoisa {
  ABERTA = 1,
  FECHADA,
}
